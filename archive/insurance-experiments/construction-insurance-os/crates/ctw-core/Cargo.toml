[package]
name = "ctw-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core types, units, and identifiers for the construction thin waist"

[features]
default = []
std = []
serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, optional = true }

[lints]
workspace = true
