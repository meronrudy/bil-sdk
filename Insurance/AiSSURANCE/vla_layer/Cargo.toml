[package]
name = "vla_layer"
version = "0.1.0"
edition = "2021"

[dependencies]
contracts = { path = "../contracts" }
shared = { path = "../shared" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

